{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": ["make init"],
    "startCommand": "hypercorn app.main:app --bind \"[::]:$PORT\""  }
}