{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": ["mkdir -p /app/db && make init && ls -lh /app/db/local.db || echo 'Arquivo não encontrado'"],
    "startCommand": "hypercorn app.main:app --bind \"[::]:$PORT\""  }
}